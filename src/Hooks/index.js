import HoverCounter from "./HoverCounter";
import ClickCounter from "./ClickCounter";

const index = () => {
  return (
    <div>
      <HoverCounter />
      <ClickCounter />
    </div>
  );
};

export default index;
